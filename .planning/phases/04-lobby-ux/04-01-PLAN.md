---
phase: 04-lobby-ux
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - DESIGN.md
autonomous: true
requirements:
  - LOBBY-08
must_haves:
  truths:
    - "DESIGN.md has a dedicated Lobby section that does not exist today"
    - "Game picker tile visual spec is fully defined (dimensions, states, colors)"
    - "Landing page section layout is specified (header, tiles, button, separator, join)"
    - "Room page and QR card layout is specified (3-col grid, per-card borders, card contents)"
    - "All color token assignments for lobby components reference existing DESIGN.md tokens"
    - "Typography assignments for every lobby text element are specified (size, weight, spacing)"
  artifacts:
    - path: "DESIGN.md"
      provides: "Lobby section with game picker, landing page, room page, QR card specs"
      contains: "## Lobby"
  key_links:
    - from: "DESIGN.md Lobby section"
      to: "04-UI-SPEC.md"
      via: "every component spec in UI-SPEC must have a corresponding entry in the Lobby section"
      pattern: "Game Picker|Landing Page|Room Page|QR Card"
---

<objective>
Extend DESIGN.md with a complete Lobby section that specifies all lobby-facing components before any HTML/CSS is written.

Purpose: D-01 requires the design spec to exist before any implementation. The current DESIGN.md covers the overlay/game HUD only. Downstream plans 04-02 and 04-03 implement HTML/CSS against this section; if the spec is absent, each plan would make independent visual decisions and produce an inconsistent result.

Output: A `## Lobby` section appended to DESIGN.md that specifies: landing page layout and section order, game picker tile states, Create Room button states, Join by code section, room page layout, QR card grid, per-card color treatment, and all typography + spacing assignments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@DESIGN.md
@.planning/phases/04-lobby-ux/04-CONTEXT.md
@.planning/phases/04-lobby-ux/04-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append Lobby section to DESIGN.md</name>
  <files>DESIGN.md</files>
  <read_first>
    - DESIGN.md — read the full file to understand the existing section structure, token names, spacing scale, button spec, and elevation levels. The Lobby section must use ONLY tokens already defined in the palette, type scale, and elevation tables. Do not invent new tokens.
    - .planning/phases/04-lobby-ux/04-UI-SPEC.md — the authoritative visual/interaction contract. Every component spec here must appear in the Lobby section.
    - .planning/phases/04-lobby-ux/04-CONTEXT.md — decisions D-01 through D-19. The Lobby section must reflect D-03 (big tiles), D-06 (tight utility feel), D-07 (section order), D-13 (3-col QR grid), D-14 (card contents), D-15 (per-card color treatment), D-16 (inline SVG QR).
  </read_first>
  <action>
Append a `## Lobby` section to the END of DESIGN.md. Do NOT modify any existing section. The section must contain exactly the following subsections (copy values from 04-UI-SPEC.md verbatim — this spec has been checker-approved and is the ground truth):

### Landing Page

Layout: max-width 480px, centered, 48px top margin, 16px horizontal body padding.

Section order (top to bottom, per D-07):
1. SPECTRE header block
2. Game picker section (primary focal point)
3. Create Room button
4. Separator
5. Join by code section

**SPECTRE header block:**
- `<h1>` text: `SPECTRE` — Inter 900 28px letter-spacing 0.12em uppercase `--text-primary`
- Tagline: `real punches. real fights.` — Inter 400 12px letter-spacing 0.08em uppercase `--text-secondary`
- Margin-bottom: 32px

**Game picker section:**
- Section label: `Select a Game` — Inter 900 12px letter-spacing 0.08em uppercase `--text-secondary`, margin-bottom 12px
- Two tiles in flex row, gap 8px, each tile flex 1, min-height 80px
- Tile default: background `--bg-surface`, border 1px `--text-dim`, border-radius 4px
- Tile label: Inter 900 16px uppercase letter-spacing 0.1em `--text-primary` centered
- Tile selected (BOXING): border 1px `--accent`, background `color-mix(in oklch, var(--accent) 10%, transparent)`
- Tile selected (DANCE): border 1px `--accent-p2`, background `color-mix(in oklch, var(--accent-p2) 10%, transparent)`
- Tile hover (unselected): border `--text-secondary`, background `--bg-mid`
- Tile active: `transform: scale(0.97)` 80ms ease-out
- Margin-bottom: 16px

**Create Room button:**
- Full-width, min-height 52px, border-radius 4px
- Disabled (no selection): background `--bg-surface`, border 1px `--text-dim`, opacity 0.5, cursor not-allowed
- Enabled: background `color-mix(in oklch, var(--accent) 15%, transparent)`, border 1px `--accent`, `--text-primary`, cursor pointer
- Hover: background `color-mix(in oklch, var(--accent) 25%, transparent)`, border `--accent-bright`
- Active: `transform: scale(0.97)` 80ms ease-out
- Label: `Create Room` — Inter 900 16px uppercase letter-spacing 0.08em
- Loading state: label `Creating...`, button disabled
- Margin-bottom: 32px

**Separator:**
- Centered `or` label — Inter 400 12px `--text-dim`. 1px horizontal rule at `--text-dim` 40% opacity either side.

**Join by code section:**
- Section label: `Join a Room` — Inter 900 12px uppercase letter-spacing 0.08em `--text-secondary`, margin-bottom 12px
- Row: text input + Join Room button, gap 8px
- Input: flex 1, min-height 52px, background `--bg-surface`, border 1px `--text-dim`, padding 16px, `--text-primary` Inter 900 16px letter-spacing 0.2em, uppercase transform, placeholder `Room Code` `--text-dim` weight 400
- Input focus: border `--accent`, outline none
- Join Room button: min-width 100px min-height 52px, default button style (bg-surface, text-dim border), text `Join Room` Inter 900 16px uppercase
- Join Room hover: border `color-mix(in oklch, var(--accent) 60%, transparent)`, background `color-mix(in oklch, var(--accent) 8%, transparent)`
- Join Room active: `transform: scale(0.97)` 80ms ease-out

### Room Page (`/rooms/{code}`)

Layout: max-width 720px, centered, 48px top margin, 24px horizontal padding.

**Header block:**
- Back link: `← Lobby` — Inter 400 16px `--text-secondary`, margin-bottom 24px, links to `/`
- Room code display (primary focal point): Inter 900 32px letter-spacing 0.2em uppercase `--text-primary`
- Game type badge: inline pill — Inter 900 12px letter-spacing 0.1em `--text-secondary`, border 1px `--text-dim`, padding 4px 8px, border-radius 4px
- Subtitle: `Share these links with your players` — Inter 400 16px `--text-secondary`
- Margin-bottom: 32px

### QR Card Grid

- CSS grid: 3 equal columns, gap 24px at viewport ≥600px; 1 column gap 16px at <600px
- Each card: background `--bg-surface`, border 1px (per-card color below), border-radius 4px, padding 24px, flex column, align-items center, gap 12px
- Elevation Level 1: `box-shadow: inset 0 1px 0 rgba(255,255,255,0.04)`
- P1 card border: 1px `--accent`
- P2 card border: 1px `--accent-p2`
- Overlay card border: 1px `color-mix(in oklch, var(--gold) 60%, transparent)`

### QR Card Contents

Each card (top to bottom):
- Role label: Inter 900 12px uppercase letter-spacing 0.1em `--text-secondary` — `PLAYER 1` / `PLAYER 2` / `OVERLAY`
- QR code: inline SVG 160px × 160px. Dark module color `#0c0809` (`--bg-deep`). Light module color `#f5efe4` (`--text-primary`). Inverted for dark theme.
- URL link: `<a href="..." target="_blank">` — Inter 900 12px `--text-secondary` letter-spacing 0.04em, word-break break-all, max 2 lines then ellipsis, text-decoration underline on hover
- Copy button: full-width, min-height 36px, background `--bg-mid`, border 1px `--text-dim`, border-radius 4px
  - Label: `Copy Link` — Inter 900 12px uppercase letter-spacing 0.08em `--text-secondary`
  - Hover: border `color-mix(in oklch, var(--accent) 60%, transparent)`, background `color-mix(in oklch, var(--accent) 8%, transparent)`
  - Success state (2000ms): border `color-mix(in oklch, var(--gold) 60%, transparent)`, label changes to `Copied!` `--text-primary`

### Error States

- **Create Room server error**: Error row below button — background `color-mix(in oklch, var(--accent-bright) 15%, transparent)`, border 1px `color-mix(in oklch, var(--accent-bright) 40%, transparent)`, padding 8px 12px, border-radius 4px. Text `--text-primary` Inter 400 16px.
- **Room not found (404)**: Centered page message. `--text-secondary` Inter 400 16px. Includes `Back to Lobby` link to `/`.

### Responsive Behavior

- Landing page: max-width 480px centered. Game picker tiles maintain 50/50 split down to 320px.
- Room page: 3-column grid collapses to 1 column at <600px viewport. Card order: P1 → P2 → Overlay.
- QR SVGs: fixed 160px × 160px at all breakpoints (no scaling).
- All touch targets: min 44px × 44px (satisfied by 52px buttons and 80px tiles).
  </action>
  <verify>
    <automated>grep -c "## Lobby" /Users/charliexue/School/Comps/claude-hackathon26/DESIGN.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "## Lobby" DESIGN.md` returns 1
    - `grep "Game Picker\|Game picker" DESIGN.md` returns at least one match
    - `grep "QR Card\|QR card" DESIGN.md` returns at least one match
    - `grep "Room Page\|Room page" DESIGN.md` returns at least one match
    - `grep "\-\-accent-p2" DESIGN.md` returns at least 2 matches (must appear in Lobby section referencing P2 tile and P2 card border — verifies that per-card color treatment D-15 is included)
    - `grep "\-\-gold" DESIGN.md | wc -l` returns 4 or more (gold already appears in existing sections; Lobby adds QR card Overlay border + copy success)
    - `grep "160px" DESIGN.md` returns at least one match (QR SVG fixed size)
    - `grep "min-height: 80px\|min-height 80px" DESIGN.md` returns at least one match (game picker tile touch target)
    - Existing DESIGN.md sections (`## Color`, `## Typography`, `## Elevation`, `## Motion`, `## Components`, `## Spacing`) all still present and unmodified
  </acceptance_criteria>
  <done>DESIGN.md contains a `## Lobby` section that fully specifies the landing page layout, game picker tiles, Create Room button, Join by code section, room page layout, QR card grid, per-card color treatment, QR card contents, error states, and responsive behavior using only existing DESIGN.md tokens. All pre-existing sections are intact.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planner → DESIGN.md | Documentation-only write; no executable code; no attack surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | DESIGN.md | accept | File is documentation; integrity enforced by git history. No runtime attack surface. |
</threat_model>

<verification>
After plan completion, verify:
1. `grep -c "## Lobby" DESIGN.md` returns exactly 1
2. All five UI-SPEC components appear in the Lobby section: game picker tiles, landing page layout, room page layout, QR card grid, QR card contents
3. No existing section in DESIGN.md was modified or removed
</verification>

<success_criteria>
DESIGN.md has a `## Lobby` section that serves as the authoritative visual spec for plans 04-02 and 04-03. Every component, color assignment, typography rule, and spacing value referenced in 04-UI-SPEC.md is captured in this section using existing DESIGN.md tokens.
</success_criteria>

<output>
After completion, create `.planning/phases/04-lobby-ux/04-01-SUMMARY.md` using the template at `@$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
