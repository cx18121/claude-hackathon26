---
phase: 07-dance-engine-protocol
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - engine/dance-plugin/src/lib.rs
autonomous: true
requirements: [DANCE-02]
gap_closure: true

must_haves:
  truths:
    - "spectator_snapshot payload includes game_type field with value 'dance'"
    - "cargo test passes for the dance-plugin crate"
  artifacts:
    - path: "engine/dance-plugin/src/lib.rs"
      provides: "spectator_snapshot returning game_type field"
      contains: '"game_type": "dance"'
  key_links:
    - from: "DancePlugin::spectator_snapshot()"
      to: "dance_snapshot wire payload"
      via: "serde_json::json! macro"
      pattern: '"game_type".*"dance"'
---

<objective>
Close DANCE-02 gap: add `"game_type": "dance"` to the `dance_snapshot` JSON payload
returned by `DancePlugin::spectator_snapshot()`.

Purpose: Spectator clients joining mid-game need to know the active game type from the
snapshot message. The field exists on `RoomSnapshot` but was never forwarded into the
plugin-level wire message, causing the DANCE-02 verification truth to fail.

Output: `engine/dance-plugin/src/lib.rs` with the field added, unit test confirming its
presence, and a green `cargo test` run.
</objective>

<execution_context>
@/Users/charliexue/School/Comps/spectre/.planning/../get-shit-done/workflows/execute-plan.md
@/Users/charliexue/School/Comps/spectre/.planning/../get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/charliexue/School/Comps/spectre/.planning/PROJECT.md
@/Users/charliexue/School/Comps/spectre/.planning/ROADMAP.md
@/Users/charliexue/School/Comps/spectre/.planning/STATE.md
@/Users/charliexue/School/Comps/spectre/.planning/phases/07-dance-engine-protocol/07-CONTEXT.md

<interfaces>
<!-- Key function in engine/dance-plugin/src/lib.rs (lines 207-219 before this fix): -->

    fn spectator_snapshot(&self, state: &dyn Any) -> Option<serde_json::Value> {
        let s = state.downcast_ref::<DanceState>()
            .expect("dance plugin: spectator_snapshot type mismatch");
        if !s.round_started || s.round_ended {
            return None;
        }
        Some(json!({
            "type": "dance_snapshot",
            "beat": s.beats_scored,
            "scores": [s.scores[0], s.scores[1]],
        }))
    }

<!-- Test module begins at line 266 in the same file. -->
<!-- Helpers available in tests mod: test_config(), make_ctx(tick, &f0, &f1, solo), empty_frames() -->
<!-- DancePlugin::new(config) and plugin.init_state() construct test state. -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add game_type to dance_snapshot and assert it in a unit test</name>
  <files>engine/dance-plugin/src/lib.rs</files>

  <read_first>
    Read engine/dance-plugin/src/lib.rs lines 207-219 (spectator_snapshot body) and
    lines 266-320 (test module helpers: test_config, make_ctx, empty_frames).
    Do NOT read any other file — all necessary context is in lib.rs.
  </read_first>

  <behavior>
    - spectator_snapshot_includes_game_type: calling spectator_snapshot() on an
      in-progress round (round_started=true, round_ended=false) returns a JSON Value
      where value["game_type"] == "dance"
    - spectator_snapshot_returns_none_before_round: calling spectator_snapshot() before
      the first on_tick (round_started=false) still returns None — existing behavior
      must be unchanged
  </behavior>

  <action>
    Step 1 — Write the failing test FIRST (RED phase).

    Inside the existing mod tests block (after the last closing brace of the last
    #[test] function at line ~567, before the final closing brace of mod tests at
    line ~568), append:

        // -----------------------------------------------------------------------
        // spectator_snapshot_includes_game_type
        // -----------------------------------------------------------------------
        #[test]
        fn spectator_snapshot_includes_game_type() {
            let plugin = DancePlugin::new(test_config());
            let mut state = plugin.init_state();
            let (f0, f1) = empty_frames();

            // Tick 1 — starts the round (sets round_started = true)
            let ctx = make_ctx(1, &f0, &f1, false);
            plugin.on_tick(&ctx, &mut *state);

            let snapshot = plugin.spectator_snapshot(&*state)
                .expect("snapshot must be Some during an active round");

            assert_eq!(
                snapshot["game_type"].as_str(),
                Some("dance"),
                "dance_snapshot payload must include game_type = 'dance'"
            );
        }

    Run: cargo test -p dance-plugin spectator_snapshot_includes_game_type
    It MUST fail (FAILED state) because the field is not yet present.

    Step 2 — Apply the production fix (GREEN phase).

    In spectator_snapshot() (around line 214), change the json! block from:

        Some(json!({
            "type": "dance_snapshot",
            "beat": s.beats_scored,
            "scores": [s.scores[0], s.scores[1]],
        }))

    to:

        Some(json!({
            "type": "dance_snapshot",
            "game_type": "dance",
            "beat": s.beats_scored,
            "scores": [s.scores[0], s.scores[1]],
        }))

    No other change to this function. Do not touch on_tick, on_round_reset, or any
    other method.

    Step 3 — Verify the full suite (REFACTOR check).

    Run: cargo test --workspace
    All tests must pass with zero failures. If a pre-existing test fails, investigate
    — do not suppress.
  </action>

  <verify>
    <automated>cargo test --workspace 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    1. grep -n '"game_type"' engine/dance-plugin/src/lib.rs
       Returns at least two matching lines: one inside spectator_snapshot (production
       fix) and one inside the new test function.
    2. cargo test -p dance-plugin spectator_snapshot_includes_game_type
       Exits 0 and output contains "test spectator_snapshot_includes_game_type ... ok".
    3. cargo test --workspace exits 0 with no failures.
    4. grep -A6 'fn spectator_snapshot' engine/dance-plugin/src/lib.rs | grep '"game_type"'
       Returns a match (confirms the field is in the production function, not only the test).
  </acceptance_criteria>

  <done>
    spectator_snapshot() returns a JSON object containing "game_type": "dance" when
    called during an active round. The new unit test passes. All workspace tests pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin->wire | dance_snapshot JSON value serialized directly to WebSocket clients |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | Information Disclosure | dance_snapshot payload | accept | game_type is a static string constant "dance"; no user-controlled data introduced by this change |
</threat_model>

<verification>
After the plan executes:

1. grep '"game_type": "dance"' engine/dance-plugin/src/lib.rs returns a match inside
   the spectator_snapshot function body.
2. cargo test --workspace exits 0.
3. The test fn spectator_snapshot_includes_game_type exists in lib.rs and passes.
</verification>

<success_criteria>
- DANCE-02 gap closed: spectator_snapshot wire message contains "game_type": "dance"
- Zero test regressions across the workspace
- Change is self-contained to a single function in a single file
</success_criteria>

<output>
After completion, create `.planning/phases/07-dance-engine-protocol/07-03-SUMMARY.md`
using the summary template.
</output>
